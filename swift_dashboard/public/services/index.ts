import apiService from './apiService';

// Export the default service
export { apiService };

// Named export for the service
export const SwiftApiService = apiService;

// Default export for easier importing
export default apiService;
